# backend/data/

Reserved for Phase 2+ datasets:

- Historical land acquisition project records
- District / state reference data
- Model training exports

No datasets are bundled in the Phase 1 build; the frontend runs entirely on
mock data located in `frontend/src/data/`.
